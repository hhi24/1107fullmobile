
document.addEventListener('DOMContentLoaded', function () {

    // ===== TOP 버튼 =====

    window.scrollToTop = function () {
        window.scrollTo({
            top: 0,
            behavior: 'smooth'
        });
    };
});
